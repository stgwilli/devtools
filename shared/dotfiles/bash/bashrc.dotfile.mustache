# bounce out if not running in interactive mode
[ -z "$PS1" ] && return

#. ~/.bash_aliases_coreutils
. ~/.bash_aliases
. ~/.bash_colors
. ~/.bash_config
. ~/.bash_env
. ~/.bash_git
. ~/.bash_git_completion

{{# core.is_osx }}
. ~/.bash_source_rvm
{{/core.is_osx}}

{{# core.is_windows }}
. ~/.bash_ssh
{{/core.is_windows }}
